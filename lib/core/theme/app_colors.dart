import 'package:flutter/material.dart';

/// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
/// SPACE APP — GEN Z COLOUR SYSTEM
/// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
class AppColors {
  AppColors._();

  // ── PRIMARY (Brand) ──
  static const primary = Color(0xFF5C4AE4);
  static const primaryLight = Color(0xFF7B6EF6);
  static const primaryPale = Color(0xFFA89BF5);
  static const primaryTint = Color(0xFFEEEDFE);
  static const primaryTextDark = Color(0xFF3C3489);

  // ── BACKGROUND ──
  static const pageBG = Color(0xFFF0EFF7);
  static const cardBG = Color(0xFFFFFFFF);
  static const surfaceBG = Color(0xFFF7F6FC);
  static const heroBG = Color(0xFF1A1A2E);

  // ── ACCENT (Streaks / Energy) ──
  static const fire = Color(0xFFFF6B35);
  static const fireTint = Color(0xFFFFF0EA);
  static const fireText = Color(0xFF99340A);

  // ── SUCCESS (Completed) ──
  static const green = Color(0xFF2DB87A);
  static const greenLight = Color(0xFFEAF3DE);
  static const greenText = Color(0xFF27500A);
  static const greenGlow = Color(0xFF9EFFD0);

  // ── DANGER (Missed) ──
  static const red = Color(0xFFF09595);
  static const redBG = Color(0xFFFCEBEB);

  // ── NEUTRAL / TEXT ──
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecond = Color(0xFF6B6B80);
  static const textMuted = Color(0xFFA0A0B0);

  // ── BORDER ──
  static const border = Color(0xFFEEEDF8);
  static const borderStrong = Color(0xFFD4D2F0);
  static const divider = Color(0xFFF3F2F9);

  // ── WEEK DOTS ──
  static const dotDone = Color(0xFF639922);
  static const dotMiss = Color(0xFFF09595);
  static const dotRest = Color(0xFFE8E6E0);
  static const dotToday = Color(0xFF5C4AE4);

  // ── STREAK BADGE ──
  static const streakBadgeBG = Color(0xFFFAEEDA);
  static const streakBadgeText = Color(0xFF633806);

  // ── DONE / MARK DONE TAGS ──
  static const doneTagBG = Color(0xFFEAF3DE);
  static const doneTagText = Color(0xFF3B6D11);
  static const markDoneTagBG = Color(0xFFEEEDFE);
  static const markDoneTagText = Color(0xFF5C4AE4);

  // ── CHECK CIRCLE ──
  static const checkDoneBG = Color(0xFF639922);
  static const checkDefaultBorder = Color(0xFFDDDDDD);

  // ── HABIT ICON CONTAINER ──
  static const habitIconBG = Color(0xFFF1EEF8);
  static const habitIconDoneBG = Color(0xFFEAF3DE);

  // ── HABIT CARD ──
  static const habitCardDoneBG = Color(0xFFF7FBF0);
  static const habitCardDoneBorder = Color(0xFFC0DD97);

  // ── BOTTOM NAV ──
  static const navBorderTop = Color(0xFFF0EEF8);
  static const navActiveIcon = Color(0xFF5C4AE4);
  static const navInactiveIcon = Color(0xFFAAAAAA);

  // ═══════════════════════════════════════════════════
  // DARK MODE
  // ═══════════════════════════════════════════════════
  static const darkPageBG = Color(0xFF0F0E1A);
  static const darkCardBG = Color(0xFF1E1D2E);
  static const darkSurfaceBG = Color(0xFF17162A);
  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextMuted = Color(0xFF8888A0);
  static const darkBorder = Color(0xFF2A2940);
  static const darkHeroBG = Color(0xFF3D31C4);

  // ═══════════════════════════════════════════════════
  // APPLE DESIGN SYSTEM
  // ═══════════════════════════════════════════════════
  static const appleGray1 = Color(0xFFF2F1F6);     // segmented control bg
  static const appleGray2 = Color(0xFFE5E5EA);     // dividers
  static const appleGray3 = Color(0xFF8E8E93);     // secondary labels
  static const appleGray4 = Color(0xFFC7C7CC);     // tertiary
  static const appleGray5 = Color(0xFFAEAEB2);     // quaternary
  static const appleBlue = Color(0xFF007AFF);       // accent / links
  static const appleGreen = Color(0xFF34C759);      // completed / success ring
  static const appleGreenLight = Color(0xFF30D158); // ring gradient end
  static const appleBG = Color(0xFFFBFBFD);         // page background
  static const appleCardBG = Color(0xFFFFFFFF);     // card surface
  static const appleLabel = Color(0xFF1C1C1E);      // primary label
  // ═══════════════════════════════════════════════════
  // MIDNIGHT DESIGN SYSTEM
  // ═══════════════════════════════════════════════════
  static const midnightPageBG       = Color(0xFF0F0E1A);
  static const midnightCardBG       = Color(0xFFFFFFFF);
  static const midnightSurfaceBG    = Color(0xFFF5F4F9);
  static const midnightHeroBG       = Color(0xFF0F0E1A);
  static const midnightPrimary      = Color(0xFF5C4AE4);
  static const midnightPrimaryLight = Color(0xFF7B6EF6);
  static const midnightPrimaryPale  = Color(0xFFA89BF5);
  static const midnightPrimaryTint  = Color(0xFFEEEDFE);
  static const midnightTextPrimary  = Color(0xFF1A1A2E);
  static const midnightTextMuted    = Color(0xFFAAAACC);
  static const midnightBorder       = Color(0xFFF0EEF8);
  static const midnightFire         = Color(0xFFFF6B35);
  static const midnightFireLight    = Color(0xFFFFF0D6);
  static const midnightFireText     = Color(0xFF854F0B);
}
